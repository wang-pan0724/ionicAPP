import { AgainstRecordPipe } from './against-record.pipe';

describe('AgainstRecordPipe', () => {
  it('create an instance', () => {
    const pipe = new AgainstRecordPipe();
    expect(pipe).toBeTruthy();
  });
});
