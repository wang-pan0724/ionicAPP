import { LeagueResultDescPipe } from './league-result-desc.pipe';

describe('LeagueResultDescPipe', () => {
  it('create an instance', () => {
    const pipe = new LeagueResultDescPipe();
    expect(pipe).toBeTruthy();
  });
});
