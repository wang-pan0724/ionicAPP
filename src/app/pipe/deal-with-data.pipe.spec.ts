import { DealWithDataPipe } from './deal-with-data.pipe';

describe('DealWithDataPipe', () => {
  it('create an instance', () => {
    const pipe = new DealWithDataPipe();
    expect(pipe).toBeTruthy();
  });
});
